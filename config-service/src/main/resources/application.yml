spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yigitucun/config-repo.git
  main:
    banner-mode: off
server:
  port: 8888
logging:
  level:
    root: warn
